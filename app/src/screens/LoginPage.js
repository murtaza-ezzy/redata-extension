import React from 'react';

function LoginPage() {
    const handleLogin = () => {

    };

    return (
        <div>
            <button onClick={handleLogin}>Login with Google</button>
        </div>
    );
}

export default LoginPage;